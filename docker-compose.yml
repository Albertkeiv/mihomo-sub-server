services:
  mihomo-sub-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_CONFIG: /app/config.yaml
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./configs:/configs:ro
    restart: unless-stopped

